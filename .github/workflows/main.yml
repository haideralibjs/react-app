name: "React App"

on:
 push:
   branches:
      - main
 workflow_dispatch:


jobs:
  build:                      
    runs-on: [self-hosted]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

  test:                     
    runs-on: [self-hosted]
    needs: build              
    steps:
      - name: Npm Install 
        run: npm ci
      - name: Npm Build
        run: npm run build
      


      
  
